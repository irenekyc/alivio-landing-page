import NavBar from "../Blocks/NavBar";
import Heading from "../Blocks/Heading";

const Hero = () => {
  return (
    <section className="w-screen h-hero">
      <NavBar />
      <Heading />
    </section>
  );
};

export default Hero;
